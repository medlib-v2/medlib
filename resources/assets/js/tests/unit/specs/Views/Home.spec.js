//import Home from '../../../../views/home/components/Home.vue';

describe('Home view', () => {
    /**
    it('should return form and datasource', () => {
        expect(Object.keys(Home.data())).toEqual(['form', 'datasource']);
    });

    describe('::methods', () => {
        it('should call this.submit', () => {
            const submit = jasmine.createSpy('submit');
            Home.submit = submit;
            Home.methods.search.call(Home);
            expect(submit).toHaveBeenCalled();
        });
    });
    **/
});