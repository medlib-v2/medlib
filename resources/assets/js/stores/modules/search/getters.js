/**
 * GET_RESULTS (state) { return state.results }
 * @param state
 */
export const getResults = state => state.results;

/**
 * GET_FILTERS (state) { return state.results }
 * @param state
 */
export const getFilters = state => state.filters;

