<?php

return array(

    // View Storage Paths
    'paths' => [
        realpath(base_path('application/views')),
    ],

    // Compiled View Path
    'compiled' => realpath(storage_path('framework/views')),

);
